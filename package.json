{
  "name": "stone-soup",
  "version": "5.0.0",
  "description": "",
  "repository": "",
  "author": "Cinnamon <cinnamon_bun_github@fastmail.com>",
  "homepage": "https://github.com/earthstar-project/stone-soup",
  "license": "AGPL-3.0",
  "main": "dist/earthstar.cjs",
  "module": "dist/earthstar.js",
  "types": "dist/types/universal.d.ts",
  "browser": {
    "./build/index.js": "./build/index.browser.js",
    "./build/crypto/crypto-driver-node.js": false,
    "tap": "tape"
  },
  "tap": {
    "nyc-arg": [
      "--exclude=build/test/*/*.js"
    ]
  },
  "exports": {
    ".": {
      "types": "./dist/types/universal.d.ts",
      "node": {
        "require": "./dist/earthstar.cjs",
        "import": "./dist/earthstar.mjs"
      },
      "default": "./dist/earthstar.js"
    },
    "./browser": {
      "types": "./dist/types/browser.d.ts",
      "default": "./dist/browser/browser.js"
    },
    "./node": {
      "types": "./dist/types/node.d.ts",
      "require": "./dist/node/node.cjs",
      "default": "./dist/node/node.mjs"
    }
  },
  "scripts": {
    "clean": "rm -rf build coverage .nyc_output browserify-tests node browser dist",
    "esbuild": "rm -rf dist && node ./scripts/build.mjs && yarn build-types",
    "build-tsc": "tsc",
    "build-types": "dts-bundle-generator --config dts-bundle.config.json",
    "clean-build-test": "yarn clean && yarn build-tsc && yarn test",
    "print-platform-support": "yarn clean && yarn build-tsc && node build/print-platform-support.js",
    "start": "node build/example-app.js",
    "browserify-tests": "mkdir -p browserify-tests && browserify build/test/universal/*.test.js build/test/browser/*.test.js build/test/browser-run-exit.js > browserify-tests/bundle.test.js",
    "browser-run-tests": "cat browserify-tests/bundle.test.js | browser-run | tap-spec",
    "test.browser": "yarn clean && yarn build-tsc && yarn browserify-tests && yarn browser-run-tests",
    "browserify-benchmark": "mkdir -p browserify-tests && browserify build/test/browser/*.benchmark.js > browserify-tests/bundle.benchmark.js",
    "browser-run-benchmark": "cat browserify-tests/bundle.benchmark.js | browser-run",
    "benchmark.browser": "yarn clean && yarn build-tsc && mkdir -p benchmark-results && yarn browserify-benchmark && yarn browser-run-benchmark | tee benchmark-results/benchmark.browser.txt",
    "test.node": "yarn test",
    "benchmark.node": "yarn clean && yarn build-tsc && mkdir -p benchmark-results && node --expose-gc build/test/node/*.benchmark.js | tee benchmark-results/benchmark.node.txt",
    "test": "tap --timeout=120 --no-bail build/test/universal/*.test.js build/test/node/*.test.js",
    "test2": "tap -Rspecy --no-bail --jobs=1 build/test/universal/*.test.js build/test/node/*.test.js",
    "test-coverage": "yarn test --coverage-report=lcov",
    "test-addresses": "tap -Rspecy --no-bail --jobs=1 build/test/universal/addresses.test.js",
    "test-bytes-and-buffers": "tap -Rspecy --no-bail --jobs=1 build/test/universal/bytes.test.js build/test/universal/buffers.test.js",
    "test-compare": "tap -Rspecy --no-bail --jobs=1 build/test/universal/compare.test.js",
    "test-crypto": "tap -Rspecy --no-bail --jobs=1 build/test/node/crypto.node.test.js",
    "test-storage": "tap -Rspecy --no-bail --jobs=1 build/test/node/storage-async.node.test.js",
    "test-storage-driver": "tap -Rspecy --no-bail --jobs=1 build/test/node/storage-driver-async.node.test.js",
    "watch-test": "onchange -i src/*.ts src/test/*.ts -- yarn clean-build-and-test",
    "prepublishOnly": "yarn clean && yarn esbuild",
    "depchart-main": "depchart  `find src | grep .ts` --exclude src/print-platform-support.ts src/decls.d.ts src/index.ts src/index.browser.ts `find src | grep '/test/'` `find src | grep '/util/'` --rankdir LR -o depchart/depchart-main --node_modules omit",
    "depchart-deps": "depchart  `find src | grep .ts` --exclude src/print-platform-support.ts src/decls.d.ts src/index.ts src/index.browser.ts `find src | grep '/test/'` `find src | grep '/util/'` --rankdir LR -o depchart/depchart-deps --node_modules integrated",
    "depchart-loose": "depchart `find src | grep .ts` --exclude src/print-platform-support.ts src/decls.d.ts src/index.ts src/index.browser.ts `find src | grep '/test/'` `find src | grep '/util/'` --loose --rankdir LR -o depchart/depchart-loose --node_modules omit",
    "depchart": "yarn depchart-main & yarn depchart-deps & yarn depchart-loose && echo 'done'"
  },
  "resolutions": {
    "minimist": "^1.2.5"
  },
  "devDependencies": {
    "@types/browser-or-node": "^1.3.0",
    "@types/node": "^14.14.43",
    "@types/tap": "^14.10.2",
    "@types/tape": "^4.13.0",
    "browser-run": "^10.1.0",
    "browserify": "^17.0.0",
    "depchart": "^1.2.0",
    "dts-bundle-generator": "^5.9.0",
    "esbuild": "^0.11.16",
    "onchange": "^7.1.0",
    "tap": "^14.11.0",
    "tap-spec": "^5.0.0",
    "tape": "^5.2.2",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "chalk": "^4.1.1",
    "chloride": "^2.4.1",
    "fast-deep-equal": "^3.1.3",
    "fast-json-stable-stringify": "^2.1.0",
    "rfc4648": "^1.4.0",
    "rfdc": "^1.2.0",
    "sha256-uint8array": "^0.10.0",
    "superbus": "^1.2.0",
    "tslib": "^2.2.0",
    "tweetnacl": "^1.0.3"
  }
}
